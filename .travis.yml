language: python
python:
  - "3.8"
install:
  - pip install pytest
  - pip install pytest-cov
  - pip install coveralls
  - pip install -r requirements.txt
script:
  - pytest --cov=.
after_success:
  - coveralls
